<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800">
    <Window.Resources>
        <CollectionViewSource x:Key="TTTaiKhoan" 
                          Source="{Binding DSTaiKhoan}" />
    </Window.Resources>
    <Grid>
        <StackPanel>
            <Label>Bảng TaiKhoan(Nếu kết nối với CSDL thành công thì sẽ có dữ liệu)</Label>
            <ListView ItemsSource="{Binding Source={StaticResource TTTaiKhoan}}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="IdNguoiDung"
                            DisplayMemberBinding="{Binding IdNguoiDung}"/>
                        <GridViewColumn Header="Username"
                            DisplayMemberBinding="{Binding Username}"/>
                        <GridViewColumn Header="Password"
                            DisplayMemberBinding="{Binding Password}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </Grid>
</Window>
