<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800">
    <Window.Resources>
        <CollectionViewSource x:Key="TTTaiKhoan" 
                          Source="{Binding DSTaiKhoan}" />
    </Window.Resources>
    <Grid Background="#FFD16969">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TreeView>
            <TreeViewItem Header="Employee 1">
                <TreeViewItem Header="Testarossa"/>
                <TreeViewItem Header="Blanc"/>
                <TreeViewItem Header="2022"/>
            </TreeViewItem>
            <TreeViewItem Header="Employee 2">
                <TreeViewItem Header="Carrera"/>
                <TreeViewItem Header="June"/>
                <TreeViewItem Header="2022"/>
            </TreeViewItem>
        </TreeView>
        <ListView Grid.Column="1" ItemsSource="{Binding Source={StaticResource TTTaiKhoan}}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="IdNguoiDung"
                                    DisplayMemberBinding="{Binding IdNguoiDung}"/>
                    <GridViewColumn Header="Username"
                                    DisplayMemberBinding="{Binding Username}"/>
                    <GridViewColumn Header="Password"
                                    DisplayMemberBinding="{Binding Password}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <ListView Grid.Column="1" Grid.Row="1" d:ItemsSource="{d:SampleData ItemCount=5}">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
